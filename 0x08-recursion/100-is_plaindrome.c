#include "main.h"

/**
 * is_plaindrome - check if a string is plaindrome.
 * @s: pointer to string.
 *
 * Return: 1 if true, 0 if false.
 */
int is_palindrome(char *s)
{
	if (*s== '\0')
	{
		return (1);
	}
	return (0);
}
