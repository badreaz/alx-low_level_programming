#include "main.h"

/**
 * is_prime_number - check if n is prime number.
 * @n: number.
 *
 * Return: 1 if prime otherwise 0.
 */
int is_prime_number(int n)
{
	int num = 1;

	if (n < 2)
		return (0);

	return (num);
}
